# EmDash encryption key (secrets at rest: admin credentials, sessions).
# A local key was generated at scaffold time in .env (not committed).
# Set the same value as a Worker secret/var in Cloudflare before CI builds:
#   wrangler secret put EMDASH_ENCRYPTION_KEY
EMDASH_ENCRYPTION_KEY=changeme
